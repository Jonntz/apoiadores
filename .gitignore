# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# env
.env
.env*.local

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# Raw campaign material — sources for scripts/prepare-assets.mjs and the font
# pipeline. Heavy, and never shipped: the optimized output lives in assets/
# and public/fonts/, which are committed.
Fotos Biancardine 2026/
neo-sans-std/
*.pdf

# Raw camera video: 194 MB at 9,6 Mbps. Git keeps every byte of it forever, and
# nothing reads it after the transcode — what ships is public/video/simoes.mp4
# (22 MB), which *is* committed because Vercel serves the repo.
assets/*.mp4
assets/*.MP4
assets/*.mov
assets/*.MOV
